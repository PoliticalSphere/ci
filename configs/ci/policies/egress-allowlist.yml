# ==============================================================================
# Political Sphere — Egress Allowlist
# ------------------------------------------------------------------------------
# Purpose:
#   Explicitly allow external hosts contacted by CI tooling.
#
# Notes:
#   - Default deny: any egress host not listed here should be rejected.
#   - Additions require a documented risk decision.
# ==============================================================================

allowlist:
  # GitHub API + releases
  - api.github.com
  - github.com
  - objects.githubusercontent.com
  - raw.githubusercontent.com

  # Python package index and artifacts (semgrep, yamllint)
  - pypi.org
  - files.pythonhosted.org

  # SonarCloud analysis
  - sonarcloud.io
